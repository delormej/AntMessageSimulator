{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build gcc",
            "type": "shell",
            "command": "gcc speed.c -std=c99 -o speed -I .",
            "args": [
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}